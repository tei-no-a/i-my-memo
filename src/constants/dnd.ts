// Drag & Drop identifiers
export const DND_PREFIX = {
    NOTE: 'note:',
    SORTABLE_NOTE: 'sortable-note:',
} as const;

export const DND_ITEM_TYPES = {
    MEMO: 'memo',
    NOTE: 'note',
    SORTABLE_NOTE: 'sortable-note',
} as const;
